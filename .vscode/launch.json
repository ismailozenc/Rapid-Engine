{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Game",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/game.exe",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "MIMode": "gdb"
        }
    ]
}